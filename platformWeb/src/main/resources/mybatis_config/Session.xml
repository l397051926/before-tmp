<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.gennlife.platform.dao.SessionMapper" >
    <delete id="clear">
        DELETE FROM session_uid
    </delete>
    <delete id="deleteByUids" >
        DELETE FROM session_uid
        where uid in
        <foreach collection="uids" item="item" index="index" open="(" separator="," close=")">
            #{item,jdbcType=INTEGER}
        </foreach>
    </delete>
    <delete id="deleteByUid" >
        DELETE FROM session_uid
        where uid =#{uid}
    </delete>

    <delete id="deleteBySessionID">
       DELETE FROM session_uid where sessionID=#{sessionID}
    </delete>

    <insert id="insertData"  >
        insert into session_uid (uid,sessionID,cTime)
        values (#{uid},#{sessionID},#{cTime})

    </insert>
    <select id="getUid" resultType="java.lang.String">
        select uid from session_uid where sessionID=#{sessionID} limit 1
    </select>
</mapper>